{
  "lock": false,
  "tasks": {
    "test": "deno test --allow-read --allow-write --allow-net --allow-env",
    // for the publish script
    "build": "deno task wasmbuild",
    "wasmbuild": "deno run -A https://deno.land/x/wasmbuild@0.15.6/main.ts --sync --features wasm"
  },
  "exclude": [
    "target"
  ]
}
